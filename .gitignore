.DS_Store
node_modules
.claude/
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

.vercel

static/sitemap.xml

# Optimized images are generated locally and committed to the repo
# so Vercel builds don't re-encode them. Run `npm run optimize-images`
# locally after adding/changing source images.

# Handbook data
static/handbook/*
handbook-data.json
.env*.local

# Local SEO research exports (CSVs from SEMrush, etc.)
keyword-research/
